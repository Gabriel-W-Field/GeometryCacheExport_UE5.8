# Third-party notices / 第三方声明

## Unreal Engine

This plugin is designed to compile against Unreal Engine 5.8 and uses Unreal Engine headers and modules such as `GeometryCache`, `AlembicLib`, and `Imath`. Unreal Engine is proprietary software and is not included in this repository. Users must comply with Epic Games' applicable Unreal Engine license and distribution terms.

本插件面向 Unreal Engine 5.8 编译，并使用 `GeometryCache`、`AlembicLib`、`Imath` 等 Unreal Engine 头文件和模块。Unreal Engine 是专有软件，本仓库不包含 Unreal Engine。使用者须遵守 Epic Games 适用的 Unreal Engine 许可和分发条款。

## Alembic and Imath

The UE5.8 engine distribution supplies the Alembic and Imath modules used by this plugin. This repository does not claim ownership of those libraries and does not redistribute their engine-supplied binaries. Their upstream license and notice information applies to the copy supplied with the user's engine installation.

UE5.8 引擎发行版提供了本插件使用的 Alembic 和 Imath 模块。本仓库不主张拥有这些库的权利，也不重新分发引擎提供的二进制文件。用户引擎安装中的对应副本应遵守上游许可证和声明。

## No bundled third-party SDK / 不包含第三方 SDK

No separate Alembic SDK, Unreal Engine installation, proprietary asset, or third-party binary is required to be committed to this repository. Do not add one merely to make local compilation convenient.

本仓库不要求提交单独的 Alembic SDK、Unreal Engine 安装、专有资产或第三方二进制。不要为了方便本地编译而将其加入仓库。
