# GeometryCacheExport_UE5.8
This is the initial editor-plugin implementation for exporting `UGeometryCache` assets to Alembic `.abc`. It adds a Content Browser context-menu command, samples tracks over time, and routes the data through a version-isolated Alembic writer.
